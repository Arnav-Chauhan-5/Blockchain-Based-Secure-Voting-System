VITE_CHAIN_ID=31337
VITE_RPC_URL=http://127.0.0.1:8545
# Optional: contract + chain are loaded from GET /api/config (deployments/localhost.json) at startup
VITE_CONTRACT_ADDRESS=
# Leave empty in dev to use Vite proxy to the relayer on the same origin
VITE_API_URL=
